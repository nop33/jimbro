{
  "name": "Jimbro",
  "user": "ubuntu",
  "install": "curl -fsSL https://vite.plus | bash && export PATH=\"$HOME/.local/share/vite-plus/bin:$PATH\" && vp install && (cd worker && vp install) && vp exec playwright install --with-deps",
  "terminals": [
    {
      "name": "dev",
      "command": "vp dev --host",
      "description": "Vite+ dev server for the Jimbro PWA on http://localhost:5173"
    }
  ],
  "ports": [
    {
      "name": "dev server",
      "port": 5173
    }
  ]
}
